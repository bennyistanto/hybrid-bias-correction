{
  "name": "hybrid-bias-correction-viz",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Interactive results companion for the LSEQM+DL daily-precipitation bias-correction thesis (Observable Framework, static, GitHub Pages).",
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "observable deploy"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.4"
  }
}
